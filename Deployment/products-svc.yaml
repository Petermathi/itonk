apiVersion: v1
kind: Service
metadata:
  labels:
    app: products
    tier: backend
    run: products
    visualize: "true"
  name: products
spec:
  ports:
  - port: 9001
    protocol: TCP
    targetPort: 9001
  selector:
    app: users
    tier: products
