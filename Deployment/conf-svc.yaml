apiVersion: v1
kind: Service
metadata:
  labels:
    app: config-service
    tier: backend
    run: config-service
    visualize: "true"
  name: config-service
spec:
  clusterIP:
  - 10.0.0.200
  ports:
  - port: 8888
    protocol: TCP
    targetPort: 8888
  selector:
    app: config-service
    tier: backend
