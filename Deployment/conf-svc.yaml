apiVersion: v1
kind: Service
metadata:
  labels:
    app: conf
    tier: backend
    run: conf
    visualize: "true"
  name: conf
spec:
  clusterIP:
  - 10.0.0.200 
  ports:
  - port: 8888
    protocol: TCP
    targetPort: 8888
  selector:
    app: conf
    tier: backend
